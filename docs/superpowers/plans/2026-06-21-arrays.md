# Layer 4.3a 数组 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement
> this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 rustj 创建并访问一维数组(`newarray`/`anewarray`/`arraylength`/
8×`*aload`/8×`*astore`),与 javac 编译的真实 `.class` 执行一致。

**Architecture:** 统一 `ArrayOop{elements:Vec<Slot>}`(每元素一槽,沿用实例字段约定)
+ `Oop::Array` 变体;新建 `interpreter/array.rs` 子模块,用 `ArrayKind` 枚举把 8 加载 /
8 存储各收敛为单函数;新增 `VmError::ArrayIndexOutOfBounds`/`NegativeArraySize`。

**Tech Stack:** Rust edition 2024,`#![deny(unsafe_code)]`,既有 `Slot`/`Reference`/`Heap`/
`OperandStack`。设计依据 `docs/superpowers/specs/2026-06-21-arrays-design.md`。

**每任务节奏:** 写失败测试 → 看红(失败原因正确)→ 最小实现 → 看绿 → 提交。
所有 `cargo` 命令在 `E:\rustj`;Windows 上忽略 incremental "拒绝访问" 噪声(非错误)。

---

### Task 1: `ArrayOop` + `Oop::Array` + 非穷尽匹配补臂

**Files:**
- Create: `src/oops/array.rs`
- Modify: `src/oops/mod.rs`、`src/oops/oop.rs`
- Modify: `src/runtime/interpreter/field.rs:189,226`、`src/runtime/interpreter/invoke.rs:346,415`
- Modify(test): `src/runtime/heap.rs` 三处 `match`

- [ ] **Step 1: 写失败测试**(新建 `src/oops/array.rs` 顶部带测试,或先只写测试)

把以下**整文件**写入 `src/oops/array.rs`(实现 + 单元测试一起;测试先在脑中应红——
因为模块还没挂进 `mod.rs`,但本步骤只验证 array.rs 自身的逻辑):

```rust
//! 一维数组(对应 HotSpot `typeArrayOop` / `objArrayOop`,本层统一表示)。
//!
//! 每个逻辑元素恰好一个 [`Slot`](long/double 也单槽,与实例字段模型一致;
//! cat-2 双槽语义仅在操作数栈/局部变量上成立)。元素类型由指令决定,不在此记录。

use crate::runtime::Slot;

/// 一维数组:元素槽位向量(每元素一槽)。
#[derive(Debug, Clone, PartialEq)]
pub struct ArrayOop {
    elements: Vec<Slot>,
}

impl ArrayOop {
    /// 由初始元素向量构造。
    pub(crate) fn new(elements: Vec<Slot>) -> Self {
        Self { elements }
    }

    /// 元素个数。
    pub fn length(&self) -> usize {
        self.elements.len()
    }

    /// 取元素槽(调用方已做越界检查)。
    pub fn element(&self, index: usize) -> Slot {
        self.elements[index]
    }

    /// 写元素槽(调用方已做越界检查)。
    pub fn set_element(&mut self, index: usize, slot: Slot) {
        self.elements[index] = slot;
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::runtime::Reference;

    #[test]
    fn new_array_length_and_defaults() {
        let a = ArrayOop::new(vec![Slot::Int(0); 3]);
        assert_eq!(a.length(), 3);
        assert_eq!(a.element(0), Slot::Int(0));
    }

    #[test]
    fn set_and_get_round_trip() {
        let mut a = ArrayOop::new(vec![Slot::Int(0); 2]);
        a.set_element(1, Slot::Int(42));
        assert_eq!(a.element(1), Slot::Int(42));
    }

    #[test]
    fn references_default_null() {
        let a = ArrayOop::new(vec![Slot::Reference(Reference::null()); 1]);
        assert_eq!(a.element(0), Slot::Reference(Reference::null()));
    }
}
```

- [ ] **Step 2: 挂模块 + 增变体**

`src/oops/mod.rs` 加:

```rust
pub mod array;
pub use array::ArrayOop;
```
(放在 `pub mod instance;` 之后)

`src/oops/oop.rs` 改为:

```rust
//! 对象指针(oop):堆中对象的标记联合(对应 HotSpot `oopDesc`)。
//!
//! 4.1 实例对象;4.3 增一维数组(统一 `ArrayOop`)。多维数组顺延。

use super::array::ArrayOop;
use super::instance::InstanceOop;

/// 堆中的对象。
#[derive(Debug, Clone, PartialEq)]
pub enum Oop {
    /// 对象实例。
    Instance(InstanceOop),
    /// 一维数组(基本类型或引用类型,统一表示)。
    Array(ArrayOop),
}
```

- [ ] **Step 3: 补非穷尽匹配**

`src/runtime/interpreter/field.rs:189`(`get_field` 的 match):

```rust
    let slot = match vm
        .heap()
        .get(objref)
        .ok_or(VmError::BadConstant("getfield 引用悬空"))?
    {
        Oop::Instance(i) => i.field(ordinal),
        Oop::Array(_) => return Err(VmError::BadConstant("getfield 目标为数组")),
    };
```

`src/runtime/interpreter/field.rs:226`(`put_field` 的 match):

```rust
    match vm
        .heap_mut()
        .get_mut(objref)
        .ok_or(VmError::BadConstant("putfield 引用悬空"))?
    {
        Oop::Instance(i) => i.set_field(ordinal, value),
        Oop::Array(_) => return Err(VmError::BadConstant("putfield 目标为数组")),
    }
```

`src/runtime/interpreter/invoke.rs` 两处 `Oop::Instance(i) => i.class_name().to_string()`
(约 346、415 行)各补臂。先读这两处上下文确认函数名后,用如下替换
(若两处文本完全相同,用 `replace_all`):

```rust
        Oop::Instance(i) => i.class_name().to_string(),
        Oop::Array(_) => {
            return Err(VmError::BadConstant("invoke 目标为数组(数组方法 clone 等顺延)"))
        }
```

`src/runtime/heap.rs` 三处测试 match 各补臂(在 `Oop::Instance(i) => { ... }` 之后):
`Oop::Array(_) => panic!("期望实例"),`。

- [ ] **Step 4: 看绿**

Run: `cargo test --lib array::tests`
Expected: PASS(3 个 ArrayOop 测试)。

Run: `cargo build`
Expected: 编译通过(所有 `match Oop` 重新穷尽)。

- [ ] **Step 5: 提交**

```bash
git add src/oops/array.rs src/oops/mod.rs src/oops/oop.rs src/runtime/interpreter/field.rs src/runtime/interpreter/invoke.rs src/runtime/heap.rs
git commit -m "feat(oops): ArrayOop + Oop::Array 变体 + 非穷尽匹配补臂"
```

---

### Task 2: `VmError` 变体 + `newarray`/`anewarray`/`arraylength` + 分派臂

**Files:**
- Create: `src/runtime/interpreter/array.rs`
- Modify: `src/runtime/interpreter/mod.rs`(VmError 变体 + Display + `mod array` + 3 分派臂)

- [ ] **Step 1: 写失败测试**(`mod.rs` 内 `#[cfg(test)] mod tests` 追加,或 array.rs 自带)

在 `src/runtime/interpreter/mod.rs` 的 `tests` 模块**末尾**(`int_narrowing_conversions` 之后)
追加(使用既有 `empty_cp`、`Frame`、`Vm`、`Value`):

```rust
    // ===== Layer 4.3a:数组分配 =====

    #[test]
    fn newarray_int_defaults_zero_and_length() {
        // bipush 3; newarray int(10); dup; arraylength; ireturn -> 3
        let code = [
            Opcode::Bipush as u8, 0x03,
            Opcode::Newarray as u8, 10, // atype=int
            Opcode::Dup as u8,
            Opcode::Arraylength as u8,
            Opcode::Ireturn as u8,
        ];
        let cp = empty_cp();
        let mut frame = Frame::new(0, 2);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(interp.interpret_with(&mut frame, &mut vm).unwrap(), Value::Int(3));
    }

    #[test]
    fn newarray_negative_count_is_negativearraysize() {
        // iconst_m1; newarray int -> NegativeArraySize
        let code = [Opcode::IconstM1 as u8, Opcode::Newarray as u8, 10];
        let cp = empty_cp();
        let mut frame = Frame::new(0, 1);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(
            interp.interpret_with(&mut frame, &mut vm).unwrap_err(),
            VmError::NegativeArraySize
        );
    }

    #[test]
    fn arraylength_on_null_is_nullpointer() {
        let code = [Opcode::AconstNull as u8, Opcode::Arraylength as u8, Opcode::Ireturn as u8];
        let cp = empty_cp();
        let mut frame = Frame::new(0, 1);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(
            interp.interpret_with(&mut frame, &mut vm).unwrap_err(),
            VmError::NullPointer
        );
    }
```

- [ ] **Step 2: 看红**

Run: `cargo test --lib newarray_int_defaults_zero_and_length newarray_negative_count arraylength_on_null`
Expected: 编译失败(`Opcode::Newarray`/`Arraylength` 未在分派循环处理 → `UnsupportedOpcode`,
且 `NegativeArraySize` 变体不存在)。确认失败原因后继续。

- [ ] **Step 3: 写 array.rs(分配部分)**

把以下写入 `src/runtime/interpreter/array.rs`:

```rust
//! 数组指令:`newarray` / `anewarray` / `arraylength` 及加载/存储(见后续任务)。
//!
//! 对应 HotSpot `bytecodeInterpreter.cpp` 的 `CASE(_newarray)` / `CASE(_anewarray)` /
//! `CASE(_arraylength)` / `CASE(_iaload)` … `CASE(_sastore)`。元素类型由指令决定,
//! `ArrayOop` 统一存 `Vec<Slot>`(不记组件类型,4.3a 不做 ArrayStoreException)。

use super::field::resolve_class_name;
use super::{Interpreter, VmError};
use crate::oops::{ArrayOop, Oop};
use crate::runtime::{Frame, Reference, Slot, Vm};

/// 数组访问种类:把 8 条加载 / 8 条存储收敛到加载/存储各一函数。
#[derive(Clone, Copy, PartialEq, Eq)]
pub(super) enum ArrayKind {
    Int,
    Long,
    Float,
    Double,
    Ref,
    Byte,
    Char,
    Short,
}

/// `newarray`:弹 count,按 atype 造默认元素数组,入堆,压引用。
pub(super) fn new_array(frame: &mut Frame, vm: &mut Vm<'_>, atype: u8) -> Result<(), VmError> {
    let count = frame.operands.pop_int()?;
    if count < 0 {
        return Err(VmError::NegativeArraySize);
    }
    let default = match atype {
        4 | 5 | 8 | 9 | 10 => Slot::Int(0), // boolean/char/byte/short/int
        6 => Slot::Float(0.0),
        7 => Slot::Double(0.0),
        11 => Slot::Long(0),
        _ => return Err(VmError::BadConstant("newarray 非法 atype")),
    };
    let elements = vec![default; count as usize];
    let r = vm.heap_mut().alloc(Oop::Array(ArrayOop::new(elements)));
    frame.operands.push_reference(r)?;
    Ok(())
}

/// `anewarray`:解析 Class(校验组件类型),弹 count,造 null 引用数组,入堆,压引用。
pub(super) fn a_new_array(
    interp: &Interpreter<'_>,
    frame: &mut Frame,
    vm: &mut Vm<'_>,
    class_index: u16,
) -> Result<(), VmError> {
    // 解析并校验为 Class 条目(组件类型名 4.3a 不存储)。
    let _component = resolve_class_name(interp.cp(), class_index)?;
    let count = frame.operands.pop_int()?;
    if count < 0 {
        return Err(VmError::NegativeArraySize);
    }
    let elements = vec![Slot::Reference(Reference::null()); count as usize];
    let r = vm.heap_mut().alloc(Oop::Array(ArrayOop::new(elements)));
    frame.operands.push_reference(r)?;
    Ok(())
}

/// `arraylength`:弹 arrayref,null 检查,压长度。
pub(super) fn array_length(frame: &mut Frame, vm: &mut Vm<'_>) -> Result<(), VmError> {
    let arrayref = frame.operands.pop_reference()?;
    if arrayref.is_null() {
        return Err(VmError::NullPointer);
    }
    let len = match vm
        .heap()
        .get(arrayref)
        .ok_or(VmError::BadConstant("arraylength 引用悬空"))?
    {
        Oop::Array(a) => a.length(),
        Oop::Instance(_) => return Err(VmError::BadConstant("arraylength 目标非数组")),
    };
    frame.operands.push_int(len as i32)?;
    Ok(())
}
```

- [ ] **Step 4: VmError 变体 + Display + 挂模块 + 分派臂**

`src/runtime/interpreter/mod.rs` 的 `VmError` 在 `StackOverflow,` 后加:

```rust
    /// ArrayIndexOutOfBoundsException:*aload/*astore 索引越界。
    ArrayIndexOutOfBounds,
    /// NegativeArraySizeException:newarray/anewarray 负长度。
    NegativeArraySize,
```

`Display` 的 `match` 在 `Self::StackOverflow => ...` 后加:

```rust
            Self::ArrayIndexOutOfBounds => write!(f, "ArrayIndexOutOfBoundsException"),
            Self::NegativeArraySize => write!(f, "NegativeArraySizeException"),
```

`mod.rs` 顶部 `mod field;` / `mod invoke;` 之后加:

```rust
mod array;
```

分派循环在 `Opcode::Invokeinterface` arm 之后、`Opcode::Return` 之前(或字段块附近),
加三个臂:

```rust
                // ---- 数组(4.3a)----
                Opcode::Newarray => {
                    let atype = self.read_u1(pc + 1)?;
                    array::new_array(frame, vm, atype)?;
                    pc += 2;
                }
                Opcode::Anewarray => {
                    let index = self.read_u2(pc + 1)?;
                    array::a_new_array(self, frame, vm, index)?;
                    pc += 3;
                }
                Opcode::Arraylength => {
                    array::array_length(frame, vm)?;
                    pc += 1;
                }
```

- [ ] **Step 5: 看绿**

Run: `cargo test --lib newarray_int_defaults_zero_and_length newarray_negative_count arraylength_on_null`
Expected: 3 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/runtime/interpreter/array.rs src/runtime/interpreter/mod.rs
git commit -m "feat(interp): newarray/anewarray/arraylength + 数组错误变体"
```

---

### Task 3: 8 条加载 `*aload`

**Files:**
- Modify: `src/runtime/interpreter/array.rs`(增 `array_load`)
- Modify: `src/runtime/interpreter/mod.rs`(8 分派臂)

- [ ] **Step 1: 写失败测试**(追加到 `mod.rs` 的 `tests`)

```rust
    // ===== Layer 4.3a:数组加载 =====

    /// 造一个 int[2]、写入、再加载:iconst_2; newarray int; dup;
    /// iconst_1(下标); bipush 7(值); iastore; iconst_1; iaload; ireturn
    /// 但存储在 Task4;此处仅测加载默认 0 + 扩展。改测扩展:
    /// 用直接构造的堆数组验证 baload/caload/saload 扩展。
    #[test]
    fn baload_sign_extends_byte() {
        use crate::oops::{ArrayOop, Oop};
        use crate::runtime::Reference;
        // 直接构造数组,放入 vm 堆;元素存 Int(200),baload 应得 -56。
        let mut vm = Vm::default();
        let arr = vm.heap_mut().alloc(Oop::Array(ArrayOop::new(vec![Slot::Int(200)])));
        // 字节码:iconst_0(下标); baload; ireturn —— 但 arrayref 要在栈底。
        // 用 local[0]=arr,aload_0 压引用。max_locals=1。
        let code = [Opcode::Aload0 as u8, Opcode::Iconst0 as u8, Opcode::Baload as u8, Opcode::Ireturn as u8];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 2);
        frame.locals.set_reference(0, arr).unwrap();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(interp.interpret_with(&mut frame, &mut vm).unwrap(), Value::Int(-56));
        let _ = Reference::null(); // 抑制未用告警
    }

    #[test]
    fn caload_zero_extends_char() {
        use crate::oops::{ArrayOop, Oop};
        let mut vm = Vm::default();
        // 存 Int(0xFFFF)(即 -1 的低16位);caload 零扩展 -> 65535
        let arr = vm.heap_mut().alloc(Oop::Array(ArrayOop::new(vec![Slot::Int(0xFFFF)])));
        let code = [Opcode::Aload0 as u8, Opcode::Iconst0 as u8, Opcode::Caload as u8, Opcode::Ireturn as u8];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 2);
        frame.locals.set_reference(0, arr).unwrap();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(interp.interpret_with(&mut frame, &mut vm).unwrap(), Value::Int(65535));
    }

    #[test]
    fn array_load_out_of_bounds_is_aioobe() {
        use crate::oops::{ArrayOop, Oop};
        let mut vm = Vm::default();
        let arr = vm.heap_mut().alloc(Oop::Array(ArrayOop::new(vec![Slot::Int(0)]))); // len 1
        // 下标 5 越界
        let code = [Opcode::Aload0 as u8, Opcode::Iconst5 as u8, Opcode::Iaload as u8, Opcode::Ireturn as u8];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 2);
        frame.locals.set_reference(0, arr).unwrap();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(
            interp.interpret_with(&mut frame, &mut vm).unwrap_err(),
            VmError::ArrayIndexOutOfBounds
        );
    }
```

- [ ] **Step 2: 看红**

Run: `cargo test --lib baload_sign_extends_byte caload_zero_extends array_load_out_of_bounds`
Expected: 编译失败(`Opcode::Baload` 等未处理 → `UnsupportedOpcode`)。确认后继续。

- [ ] **Step 3: 写 `array_load`**

`src/runtime/interpreter/array.rs` 末尾(在 `array_length` 之后)加:

```rust
/// `*aload`:弹 index、弹 arrayref,null + 越界检查,按种类压值(byte/char/short 扩展)。
pub(super) fn array_load(
    frame: &mut Frame,
    vm: &mut Vm<'_>,
    kind: ArrayKind,
) -> Result<(), VmError> {
    let index = frame.operands.pop_int()?;
    let arrayref = frame.operands.pop_reference()?;
    if arrayref.is_null() {
        return Err(VmError::NullPointer);
    }
    let slot = match vm
        .heap()
        .get(arrayref)
        .ok_or(VmError::BadConstant("aload 引用悬空"))?
    {
        Oop::Array(a) => {
            if index < 0 || (index as usize) >= a.length() {
                return Err(VmError::ArrayIndexOutOfBounds);
            }
            a.element(index as usize)
        }
        Oop::Instance(_) => return Err(VmError::BadConstant("aload 目标非数组")),
    };
    push_array_value(frame, kind, slot)
}

/// 按种类把槽值压回操作数栈(byte/char/short 在此扩展;类型不符报错)。
fn push_array_value(frame: &mut Frame, kind: ArrayKind, slot: Slot) -> Result<(), VmError> {
    match kind {
        ArrayKind::Int => {
            let Slot::Int(v) = slot else {
                return Err(VmError::BadConstant("iaload 元素非 int"));
            };
            frame.operands.push_int(v)?;
        }
        ArrayKind::Long => {
            let Slot::Long(v) = slot else {
                return Err(VmError::BadConstant("laload 元素非 long"));
            };
            frame.operands.push_long(v)?;
        }
        ArrayKind::Float => {
            let Slot::Float(v) = slot else {
                return Err(VmError::BadConstant("faload 元素非 float"));
            };
            frame.operands.push_float(v)?;
        }
        ArrayKind::Double => {
            let Slot::Double(v) = slot else {
                return Err(VmError::BadConstant("daload 元素非 double"));
            };
            frame.operands.push_double(v)?;
        }
        ArrayKind::Ref => {
            let Slot::Reference(r) = slot else {
                return Err(VmError::BadConstant("aaload 元素非引用"));
            };
            frame.operands.push_reference(r)?;
        }
        ArrayKind::Byte => {
            let Slot::Int(v) = slot else {
                return Err(VmError::BadConstant("baload 元素非 int 槽"));
            };
            frame.operands.push_int((v as i8) as i32)?;
        }
        ArrayKind::Char => {
            let Slot::Int(v) = slot else {
                return Err(VmError::BadConstant("caload 元素非 int 槽"));
            };
            frame.operands.push_int((v as u16) as i32)?;
        }
        ArrayKind::Short => {
            let Slot::Int(v) = slot else {
                return Err(VmError::BadConstant("saload 元素非 int 槽"));
            };
            frame.operands.push_int((v as i16) as i32)?;
        }
    }
    Ok(())
}
```

`array.rs` 顶部 `use` 增(已有则跳过):`use crate::runtime::Slot;`(已含)。

- [ ] **Step 4: 加载分派臂**

`mod.rs` 分派循环在 `Opcode::Arraylength` arm 之后加:

```rust
                Opcode::Iaload => {
                    array::array_load(frame, vm, array::ArrayKind::Int)?;
                    pc += 1;
                }
                Opcode::Laload => {
                    array::array_load(frame, vm, array::ArrayKind::Long)?;
                    pc += 1;
                }
                Opcode::Faload => {
                    array::array_load(frame, vm, array::ArrayKind::Float)?;
                    pc += 1;
                }
                Opcode::Daload => {
                    array::array_load(frame, vm, array::ArrayKind::Double)?;
                    pc += 1;
                }
                Opcode::Aaload => {
                    array::array_load(frame, vm, array::ArrayKind::Ref)?;
                    pc += 1;
                }
                Opcode::Baload => {
                    array::array_load(frame, vm, array::ArrayKind::Byte)?;
                    pc += 1;
                }
                Opcode::Caload => {
                    array::array_load(frame, vm, array::ArrayKind::Char)?;
                    pc += 1;
                }
                Opcode::Saload => {
                    array::array_load(frame, vm, array::ArrayKind::Short)?;
                    pc += 1;
                }
```

- [ ] **Step 5: 看绿**

Run: `cargo test --lib baload_sign_extends_byte caload_zero_extends array_load_out_of_bounds`
Expected: 3 PASS。

Run: `cargo test --lib`
Expected: 全部既有 + 新增 PASS(无回归)。

- [ ] **Step 6: 提交**

```bash
git add src/runtime/interpreter/array.rs src/runtime/interpreter/mod.rs
git commit -m "feat(interp): 8 条数组加载指令(iaload..saload,byte/char/short 扩展)"
```

---

### Task 4: 8 条存储 `*astore`

**Files:**
- Modify: `src/runtime/interpreter/array.rs`(增 `array_store`)
- Modify: `src/runtime/interpreter/mod.rs`(8 分派臂)

- [ ] **Step 1: 写失败测试**(追加到 `mod.rs` 的 `tests`)

```rust
    // ===== Layer 4.3a:数组存储 =====

    /// newarray int[2]; dup; 存 arr 到 local0;
    /// 然后:aload_0; iconst_1(下标); bipush 42(值); iastore;
    ///       aload_0; iconst_1; iaload; ireturn  -> 42
    #[test]
    fn iastore_then_iaload_round_trip() {
        let code = [
            Opcode::Iconst2 as u8,            // count=2
            Opcode::Newarray as u8, 10,       // int[2]
            Opcode::Astore0 as u8,            // arr -> local0
            Opcode::Aload0 as u8,             // arr
            Opcode::Iconst1 as u8,            // index=1
            Opcode::Bipush as u8, 0x2A,       // value=42
            Opcode::Iastore as u8,
            Opcode::Aload0 as u8,             // arr
            Opcode::Iconst1 as u8,            // index=1
            Opcode::Iaload as u8,
            Opcode::Ireturn as u8,
        ];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 3);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(interp.interpret_with(&mut frame, &mut vm).unwrap(), Value::Int(42));
    }

    /// bastore 存 200,baload 读回 -56(符号扩展)
    #[test]
    fn bastore_baload_sign_extension() {
        let code = [
            Opcode::Iconst1 as u8,            // count=1
            Opcode::Newarray as u8, 8,        // byte[1]
            Opcode::Astore0 as u8,
            Opcode::Aload0 as u8,
            Opcode::Iconst0 as u8,            // index=0
            Opcode::Sipush as u8, 0x00, 0xC8, // value=200
            Opcode::Bastore as u8,
            Opcode::Aload0 as u8,
            Opcode::Iconst0 as u8,
            Opcode::Baload as u8,
            Opcode::Ireturn as u8,
        ];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 3);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(interp.interpret_with(&mut frame, &mut vm).unwrap(), Value::Int(-56));
    }

    /// astore 越界 -> ArrayIndexOutOfBounds
    #[test]
    fn iastore_out_of_bounds_is_aioobe() {
        let code = [
            Opcode::Iconst1 as u8,            // count=1
            Opcode::Newarray as u8, 10,       // int[1]
            Opcode::Aload0 => 0, // 占位,删除
        ];
        // (上面占位会被替换为真实序列,见 Step3 代码;此处仅占位看红)
        let _ = code;
        // 真正测试:见下面实现完后的版本。
        // —— 为保证“先红”,此测试体在 Step3 实现后会改写。
        todo!("替换为完整序列")
    }
```

> **注意:** 上面第三个测试含 `todo!`,仅为 Step2"看红"用;Step3 实现后改写为真实越界序列:

把第三个测试**整体替换**为(删除 todo 版):

```rust
    #[test]
    fn iastore_out_of_bounds_is_aioobe() {
        // int[1]; 存到 index 5 -> AIOOBE
        let code = [
            Opcode::Iconst1 as u8,
            Opcode::Newarray as u8, 10,       // int[1]
            Opcode::Astore0 as u8,
            Opcode::Aload0 as u8,             // arr
            Opcode::Iconst5 as u8,            // index=5 越界
            Opcode::Iconst9 as u8,            // value=9
            Opcode::Iastore as u8,
            Opcode::Iconst0 as u8,
            Opcode::Ireturn as u8,
        ];
        let cp = empty_cp();
        let mut frame = Frame::new(1, 3);
        let mut vm = Vm::default();
        let interp = Interpreter::new(&code, &cp);
        assert_eq!(
            interp.interpret_with(&mut frame, &mut vm).unwrap_err(),
            VmError::ArrayIndexOutOfBounds
        );
    }
```

- [ ] **Step 2: 看红**

Run: `cargo test --lib iastore_then_iaload_round_trip bastore_baload_sign_extension iastore_out_of_bounds`
Expected: 编译失败(`Opcode::Iastore`/`Bastore` 未处理)。(注意:先不要展开第三个 todo
版——它在 Step3 前无法编译。)**为简化,先删第三个测试,只让前两个看红**;
确认前两个 `UnsupportedOpcode` 后,在 Step3 实现完,再把第三个真实版加上看绿。

> 实务:执行时把"第三个 todo 版"先不写入,只写前两个看红;Step3 实现后再追加第三个。

- [ ] **Step 3: 写 `array_store`**

`src/runtime/interpreter/array.rs` 末尾加:

```rust
/// `*astore`:弹 value、弹 index、弹 arrayref,null + 越界检查后写。
/// byte/char/short 存原始 int(扩展统一推迟到加载侧)。
pub(super) fn array_store(
    frame: &mut Frame,
    vm: &mut Vm<'_>,
    kind: ArrayKind,
) -> Result<(), VmError> {
    let value = pop_array_value(frame, kind)?;
    let index = frame.operands.pop_int()?;
    let arrayref = frame.operands.pop_reference()?;
    if arrayref.is_null() {
        return Err(VmError::NullPointer);
    }
    let idx = if index < 0 {
        return Err(VmError::ArrayIndexOutOfBounds);
    } else {
        index as usize
    };
    match vm
        .heap_mut()
        .get_mut(arrayref)
        .ok_or(VmError::BadConstant("astore 引用悬空"))?
    {
        Oop::Array(a) => {
            if idx >= a.length() {
                return Err(VmError::ArrayIndexOutOfBounds);
            }
            a.set_element(idx, value);
        }
        Oop::Instance(_) => return Err(VmError::BadConstant("astore 目标非数组")),
    }
    Ok(())
}

/// 按种类弹栈取值(byte/char/short 取 int;cat-2 取 long/double)。
fn pop_array_value(frame: &mut Frame, kind: ArrayKind) -> Result<Slot, VmError> {
    Ok(match kind {
        ArrayKind::Int | ArrayKind::Byte | ArrayKind::Char | ArrayKind::Short => {
            Slot::Int(frame.operands.pop_int()?)
        }
        ArrayKind::Long => Slot::Long(frame.operands.pop_long()?),
        ArrayKind::Float => Slot::Float(frame.operands.pop_float()?),
        ArrayKind::Double => Slot::Double(frame.operands.pop_double()?),
        ArrayKind::Ref => Slot::Reference(frame.operands.pop_reference()?),
    })
}
```

- [ ] **Step 4: 存储分派臂**

`mod.rs` 分派循环在 8 个 `*aload` 臂之后加:

```rust
                Opcode::Iastore => {
                    array::array_store(frame, vm, array::ArrayKind::Int)?;
                    pc += 1;
                }
                Opcode::Lastore => {
                    array::array_store(frame, vm, array::ArrayKind::Long)?;
                    pc += 1;
                }
                Opcode::Fastore => {
                    array::array_store(frame, vm, array::ArrayKind::Float)?;
                    pc += 1;
                }
                Opcode::Dastore => {
                    array::array_store(frame, vm, array::ArrayKind::Double)?;
                    pc += 1;
                }
                Opcode::Aastore => {
                    array::array_store(frame, vm, array::ArrayKind::Ref)?;
                    pc += 1;
                }
                Opcode::Bastore => {
                    array::array_store(frame, vm, array::ArrayKind::Byte)?;
                    pc += 1;
                }
                Opcode::Castore => {
                    array::array_store(frame, vm, array::ArrayKind::Char)?;
                    pc += 1;
                }
                Opcode::Sastore => {
                    array::array_store(frame, vm, array::ArrayKind::Short)?;
                    pc += 1;
                }
```

- [ ] **Step 5: 看绿**(此时追加第三个越界测试的真实版)

Run: `cargo test --lib iastore_then_iaload_round_trip bastore_baload_sign_extension iastore_out_of_bounds_is_aioobe`
Expected: 3 PASS。

Run: `cargo test --lib`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add src/runtime/interpreter/array.rs src/runtime/interpreter/mod.rs
git commit -m "feat(interp): 8 条数组存储指令(iastore..sastore)"
```

---

### Task 5: javac 集成闸门

**Files:**
- Create: `tests/arrays.rs`

- [ ] **Step 1: 写失败测试**(整文件)

把以下写入 `tests/arrays.rs`(沿用 `interface_dispatch.rs` 的编译/加载骨架):

```rust
//! 集成闸门:用 `javac` 编译使用各类型数组的真实 Java,解析其 `.class`,
//! 用 rustj 真正执行,验证 newarray/anewarray/arraylength/*aload/*astore 与 JVM 一致。
//! 无 javac 则跳过。

use std::process::Command;

use rustj::classfile::parse;
use rustj::constant_pool::ConstantPoolEntry;
use rustj::metadata::{ClassFile, MethodInfo};
use rustj::oops::ClassRegistry;
use rustj::runtime::{Frame, Interpreter, Value, Vm, VmError};

fn javac_available() -> bool {
    Command::new("javac")
        .arg("-version")
        .output()
        .map(|o| o.status.success())
        .unwrap_or(false)
}

static COMPILE_SEQ: std::sync::atomic::AtomicU64 = std::sync::atomic::AtomicU64::new(0);

fn compile_and_load_all(source: &str, public_name: &str) -> ClassRegistry {
    let seq = COMPILE_SEQ.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
    let dir = std::env::temp_dir().join(format!("rustj-arr-{}-{seq}-{public_name}", std::process::id()));
    let _ = std::fs::remove_dir_all(&dir);
    std::fs::create_dir_all(&dir).unwrap();
    let src = dir.join(format!("{public_name}.java"));
    std::fs::write(&src, source).unwrap();
    let output = Command::new("javac")
        .arg("-d")
        .arg(&dir)
        .arg(&src)
        .output()
        .expect("javac 执行失败");
    assert!(output.status.success(), "javac 编译失败:\n{}", String::from_utf8_lossy(&output.stderr));
    let mut registry = ClassRegistry::new();
    for entry in std::fs::read_dir(&dir).unwrap() {
        let path = entry.unwrap().path();
        if path.extension().and_then(|e| e.to_str()) == Some("class") {
            let bytes = std::fs::read(&path).unwrap();
            let cf = parse(&bytes).expect("解析应成功");
            registry.load(cf).expect("加载应成功");
        }
    }
    let _ = std::fs::remove_dir_all(&dir);
    registry
}

fn utf8(cf: &ClassFile, index: u16) -> String {
    match cf.constant_pool.get(index).unwrap() {
        ConstantPoolEntry::Utf8(s) => s.clone(),
        e => panic!("expected Utf8 at {index}, got {e:?}"),
    }
}

fn find_method<'a>(cf: &'a ClassFile, name: &str, desc: &str) -> &'a MethodInfo {
    cf.methods
        .iter()
        .find(|m| utf8(cf, m.name_index) == name && utf8(cf, m.descriptor_index) == desc)
        .unwrap_or_else(|| panic!("未找到方法 {name}{desc}"))
}

fn run(registry: &ClassRegistry, class_name: &str, name: &str, desc: &str) -> Value {
    let lc = registry.get(class_name).unwrap_or_else(|| panic!("类 {class_name} 未加载"));
    let method = find_method(&lc.cf, name, desc);
    let code = method.code.as_ref().unwrap_or_else(|| panic!("{name} 应有 Code"));
    let mut frame = Frame::new(code.max_locals, code.max_stack);
    let interp = Interpreter::new(&code.code, &lc.cf.constant_pool);
    let mut vm = Vm::new(registry);
    interp.interpret_with(&mut frame, &mut vm).unwrap_or_else(|e| panic!("{name}{desc} 执行失败:{e}"))
}

const SOURCE: &str = r#"
public class Arrays {
    // int[] 求和:1+2+...+5 = 15
    public static int sumInts() {
        int[] a = new int[5];
        for (int i = 0; i < 5; i++) a[i] = i + 1;
        int s = 0;
        for (int i = 0; i < a.length; i++) s += a[i];
        return s;
    }
    // byte[] 符号扩展:存 200 读回 -56
    public static int byteRoundTrip() {
        byte[] b = new byte[1];
        b[0] = (byte) 200;
        return b[0];
    }
    // char[] 零扩展:存 0xFFFF 读回 65535
    public static int charRoundTrip() {
        char[] c = new char[1];
        c[0] = (char) 0xFFFF;
        return c[0];
    }
    // long[] 求和:10 + 20 = 30
    public static long sumLongs() {
        long[] a = new long[2];
        a[0] = 10L; a[1] = 20L;
        return a[0] + a[1];
    }
    // double[] 读写:2.5 + 1.5 = 4.0
    public static double sumDoubles() {
        double[] a = new double[2];
        a[0] = 2.5; a[1] = 1.5;
        return a[0] + a[1];
    }
    // 引用数组(Object[]):默认 null;赋值后非 null
    public static int refArray() {
        Object[] a = new Object[2];
        if (a[0] != null) return -1;       // 默认 null
        Object o = new Object();
        a[1] = o;
        return a[1] == null ? -1 : a.length; // 长度 2
    }
    // arraylength
    public static int lengthOf() {
        int[] a = new int[7];
        return a.length;
    }
}
"#;

#[test]
fn sum_int_array() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "sumInts", "()I"), Value::Int(15));
}

#[test]
fn byte_array_sign_extension() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "byteRoundTrip", "()I"), Value::Int(-56));
}

#[test]
fn char_array_zero_extension() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "charRoundTrip", "()I"), Value::Int(65535));
}

#[test]
fn long_array_sum() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "sumLongs", "()J"), Value::Long(30));
}

#[test]
fn double_array_sum() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    if let Value::Double(v) = run(&reg, "Arrays", "sumDoubles", "()D") {
        assert!((v - 4.0).abs() < 1e-9);
    } else { panic!("期望 double"); }
}

#[test]
fn reference_array_default_and_assign() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "refArray", "()I"), Value::Int(2));
}

#[test]
fn array_length() {
    if !javac_available() { eprintln!("跳过:未找到 javac"); return; }
    let reg = compile_and_load_all(SOURCE, "Arrays");
    assert_eq!(run(&reg, "Arrays", "lengthOf", "()I"), Value::Int(7));
}
```

- [ ] **Step 2: 看红→看绿**

Run: `cargo test --test arrays`
Expected: 若有 javac,7 PASS;若无 javac,全部跳过。

> **关键验证点:** 这是"能否跑通真实字节码"判据。若 `sumInts` 等失败,最可能的原因是
> (a)实参序(本层数组指令不涉及 invoke 实参,排除)、(b)cat-2 数组元素 long/double 的
> 栈双槽还原(`push_long`/`push_double` 已处理)、(c)`new`/`getfield`/`putfield` 被数组误命中
> (Task1 的补臂已挡)。逐个断言失败时定位。

- [ ] **Step 3: 提交**

```bash
git add tests/arrays.rs
git commit -m "test: Layer 4.3a 数组 javac 集成闸门"
```

---

### Task 6: 终验

- [ ] **Step 1: 全量测试**

Run: `cargo test`
Expected: 全绿(单元 + 集成)。

- [ ] **Step 2: clippy 零告警**

Run: `cargo clippy --all-targets -- -D warnings`
Expected: 无输出(零告警)。零 unsafe。

- [ ] **Step 3: 更新项目记忆**

更新 `C:\Users\jinha\.claude\projects\E--rustj\memory\hotspot-rust-migration-project.md`:
Layer 4 增 4.3a 完成条;下一步改为 4.3b(multianewarray)或异常处理(`athrow`/异常表)。

- [ ] **Step 4: 提交(若有记忆文件改动)**

```bash
git add -A && git commit -m "docs(memory): 记录 4.3a 数组完成"
```
(记忆文件在仓库外,通常无需提交;仅当 docs 内有改动时。)

---

## 自检(写完后回看)

- **spec 覆盖:** newarray(§3.1)✓ anewarray(§3.2)✓ arraylength(§3.3)✓ 8 加载(§3.4)✓
  8 存储(§3.5)✓ 错误(§4)✓ 补臂(§6)✓ 顺延(§7)✓。
- **类型一致:** `ArrayKind` 8 变体 ↔ 加载/存储表各 8 行;`array_load`/`array_store`
  签名与 `field.rs` 同形(`frame, vm, ...`);`VmError::ArrayIndexOutOfBounds`/
  `NegativeArraySize` 在 mod.rs 与 array.rs 一致拼写。
- **占位符:** 无 TBD;Task4 Step1 的 todo 版已在 Step3 替换说明(执行时直接写真实版即可)。
